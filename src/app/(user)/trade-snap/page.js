import TradeSnap from '@/rendering/tradeSnap';

export const metadata = {
    title: 'AI Trade (TradeSnap) | ChronosX',
    description: 'Instant OCR chart reading and algorithmic structure grading from MT5 and TradingView screenshots with complete risk/reward calculations.',
};

export default function TradeSnapPage() {
    return <TradeSnap />;
}
